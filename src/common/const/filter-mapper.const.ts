import {
  Any,
  ArrayContainedBy,
  ArrayContains,
  ArrayOverlap,
  Between,
  Equal,
  ILike,
  In,
  IsNull,
  LessThan,
  LessThanOrEqual,
  Like,
  MoreThan,
  MoreThanOrEqual,
  Not,
} from 'typeorm';

export const FILTER_MAPPER = {
  not: Not,
  equal: Equal,
  less_than: LessThan,
  less_than_or_equal: LessThanOrEqual,
  more_than: MoreThan,
  more_than_or_equal: MoreThanOrEqual,
  in: In,
  like: Like,
  i_like: ILike,
  is_null: IsNull,
  array_contains: ArrayContains,
  array_overlaps: ArrayOverlap,
  any: Any,
  array_contained_by: ArrayContainedBy,
  between: Between,
};
